/***********************
* Adobe Edge Animate Composition Actions
*
* Edit this file with caution, being careful to preserve 
* function signatures and comments starting with 'Edge' to maintain the 
* ability to interact with these actions from within Adobe Edge Animate
*
***********************/
(function($, Edge, compId){
var Composition = Edge.Composition, Symbol = Edge.Symbol; // aliases for commonly used Edge classes

   //Edge symbol: 'stage'
   (function(symbolName) {
      
      
      Symbol.bindElementAction(compId, symbolName, "${walker_Converted_480}", "click", function(sym, e) {
         
         // Replay a video track from the beginning, regardless of current playing state 
         sym.$("walker_Converted_480")[0].currentTime = 0;
         if (sym.$("walker_Converted_480")[0].paused) {
         	sym.$("walker_Converted_480")[0].play();
         }
         

      });
      //Edge binding end

      Symbol.bindElementAction(compId, symbolName, "${boombox_Converted_480}", "click", function(sym, e) {
         // insert code for mouse click here
         // Replay a video track from the beginning, regardless of current playing state 
         sym.$("boombox_Converted_480")[0].currentTime = 0;
         if (sym.$("boombox_Converted_480")[0].paused) {
         	sym.$("boombox_Converted_480")[0].play();
         }
         

      });
      //Edge binding end

      Symbol.bindElementAction(compId, symbolName, "${boombox_Converted_480}", "dblclick", function(sym, e) {
         // insert code for mouse double click here
         
         // Pause a video track 
         sym.$("boombox_Converted_480")[0].pause();
         

      });
      //Edge binding end

      Symbol.bindElementAction(compId, symbolName, "${shooter_Converted_480}", "click", function(sym, e) {
         // insert code for mouse click here
         // Replay a video track from the beginning, regardless of current playing state 
         sym.$("shooter_Converted_480")[0].currentTime = 0;
         if (sym.$("shooter_Converted_480")[0].paused) {
         	sym.$("shooter_Converted_480")[0].play();
         }
         

      });
      //Edge binding end

      Symbol.bindElementAction(compId, symbolName, "${shooter_Converted_480}", "dblclick", function(sym, e) {
         // insert code for mouse double click here
         // Pause a video track 
         sym.$("shooter_Converted_480")[0].pause();
         

      });
      //Edge binding end

      Symbol.bindElementAction(compId, symbolName, "${Eater_Converted_480}", "click", function(sym, e) {
         // insert code for mouse click here
         // Replay a video track from the beginning, regardless of current playing state 
         sym.$("Eater_Converted_480")[0].currentTime = 0;
         if (sym.$("Eater_Converted_480")[0].paused) {
         	sym.$("Eater_Converted_480")[0].play();
         }
         

      });
      //Edge binding end

      Symbol.bindElementAction(compId, symbolName, "${Eater_Converted_480}", "dblclick", function(sym, e) {
         // insert code for mouse double click here
         // Pause a video track 
         sym.$("Eater_Converted_480")[0].pause();
         

      });
      //Edge binding end

      Symbol.bindElementAction(compId, symbolName, "${moneybag_Converted_480}", "click", function(sym, e) {
         // insert code for mouse click here
         // Replay a video track from the beginning, regardless of current playing state 
         sym.$("moneybag_Converted_480")[0].currentTime = 0;
         if (sym.$("moneybag_Converted_480")[0].paused) {
         	sym.$("moneybag_Converted_480")[0].play();
         }
         

      });
      //Edge binding end

      Symbol.bindElementAction(compId, symbolName, "${rockon_Converted_480}", "click", function(sym, e) {
         // insert code for mouse click here
         // Replay a video track from the beginning, regardless of current playing state 
         sym.$("rockon_Converted_480")[0].currentTime = 0;
         if (sym.$("rockon_Converted_480")[0].paused) {
         	sym.$("rockon_Converted_480")[0].play();
         }
         

      });
      //Edge binding end

      Symbol.bindElementAction(compId, symbolName, "${rockon_Converted_480}", "dblclick", function(sym, e) {
         // insert code for mouse double click here
         // Pause a video track 
         sym.$("rockon_Converted_480")[0].pause();
         

      });
      //Edge binding end

      Symbol.bindElementAction(compId, symbolName, "${treasurechest_Converted_480}", "click", function(sym, e) {
         // insert code for mouse click here
         // Replay a video track from the beginning, regardless of current playing state 
         sym.$("treasurechest_Converted_480")[0].currentTime = 0;
         if (sym.$("treasurechest_Converted_480")[0].paused) {
         	sym.$("treasurechest_Converted_480")[0].play();
         }
         

      });
      //Edge binding end

      Symbol.bindElementAction(compId, symbolName, "${thinkbubble_Converted_480}", "click", function(sym, e) {
         // insert code for mouse click here
         // Replay a video track from the beginning, regardless of current playing state 
         sym.$("thinkbubble_Converted_480")[0].currentTime = 0;
         if (sym.$("thinkbubble_Converted_480")[0].paused) {
         	sym.$("thinkbubble_Converted_480")[0].play();
         }
         

      });
      //Edge binding end

      Symbol.bindElementAction(compId, symbolName, "${mpc_Converted_480}", "click", function(sym, e) {
         // insert code for mouse click here
         // Replay a video track from the beginning, regardless of current playing state 
         sym.$("mpc_Converted_480")[0].currentTime = 0;
         if (sym.$("mpc_Converted_480")[0].paused) {
         	sym.$("mpc_Converted_480")[0].play();
         }
         

      });
      //Edge binding end

      Symbol.bindElementAction(compId, symbolName, "${treasurechest_Converted_480}", "dblclick", function(sym, e) {
         // insert code for mouse double click here
         // Pause a video track 
         sym.$("treasurechest_Converted_480")[0].pause();
         

      });
      //Edge binding end

      Symbol.bindElementAction(compId, symbolName, "${walker_Converted_480}", "dblclick", function(sym, e) {
         // insert code for mouse double click here
         // Pause a video track 
         sym.$("walker_Converted_480")[0].pause();
         

      });
      //Edge binding end

      Symbol.bindElementAction(compId, symbolName, "${thinkbubble_Converted_480}", "dblclick", function(sym, e) {
         // insert code for mouse double click here
         // Pause a video track 
         sym.$("thinkbubble_Converted_480")[0].pause();
         

      });
      //Edge binding end

      Symbol.bindElementAction(compId, symbolName, "${mpc_Converted_480}", "dblclick", function(sym, e) {
         // insert code for mouse double click here
         // Pause a video track 
         sym.$("mpc_Converted_480")[0].pause();
         

      });
      //Edge binding end

      Symbol.bindElementAction(compId, symbolName, "${moneybag_Converted_480}", "dblclick", function(sym, e) {
         // insert code for mouse double click here
         // Pause a video track 
         sym.$("moneybag_Converted_480")[0].pause();
         

      });
      //Edge binding end

   })("stage");
   //Edge symbol end:'stage'

})(window.jQuery || AdobeEdge.$, AdobeEdge, "EDGE-55141061");